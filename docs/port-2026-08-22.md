# Port pack — 2026-08-22 audit additions

**Merge direction: ACCOUNT-BASE WINS.** Ben's live account skills are the base.
This repo branch is a divergent fork built on the 2026-07-29 snapshot; it does NOT
contain the Aug 3–22 account work (RULE 0.5 geography gate, Workday Daily Digest
parsing, Gmail rejection queries, D1-or-stop standards rule, current Closing Day
descriptor, references/search-pathways.md, ben-career-counselor). Nothing in the
repo copies supersedes that content.

Port ONLY the four additions below onto the account skills. Do not copy whole files.

## Do NOT overwrite from this branch
- `resume-builder/SKILL.md` and `resume_components.js` — they carry the RETIRED
  Closing Day descriptor ("sales pipeline analytics platform for real estate teams"),
  which is on D1's bannedPhrases list. Account copy wins. Take ONLY the
  `verify_resume.py` guard from item 3.
- `jobs-api/SKILL.md` and `job-application-builder/SKILL.md` — both still reference
  `job-search.json` and `roadmap.benwhetstone.info`, retired since 2026-08-03.
  Account copies win; take only the item-1/2/4 hunks.
- `career-counselor/` — this branch still ships it. It is the redundant skill flagged
  for deletion. Keep `ben-career-counselor`; do not resurrect this one.

---

## 1. Pay band — $77,000–$100,000, floor $77,000

Rule: floor $77,000; if the posting's own band runs higher, answer inside the
posting's band. Never a figure below $77,000.

RETIRED, never use again: $65K floor · $75–85K ask · $70,001–$80,000 band pick ·
~$60K break-even · $54K hard drop · $65–70K Phase 1 default · $78K · $63K · $60K target.

Apply at these places in the account skills:

**job-search-sweep — Pay Floor section:**
```
Band: $77,000-$100,000. Floor: $77,000 (updated 2026-08-22). A posting whose band
ENTIRELY sits below $77K is a drop. A band that straddles the floor stays on the list
flagged "below floor, negotiation candidate". If a posting's own band runs higher than
ours, that's fine - salary answers go inside the posting's band.
```
Also update the hard-drop line ("a posted band entirely under $77K"), the card **Pay:**
line, and the qualification checklist line ("Is the posted band at least partly at/above
the $77K floor? (Entirely below: STOP.)").

**job-application-builder — screening-question step:**
```
For salary: $77,000-$100,000, floor $77,000 (updated 2026-08-22). If the posting's own
band runs higher, answer inside the posting's band. Never a figure below $77,000.
Numbers only - no ranges in a single-value field.
```

**research-analyst — market-comparison line:** `($77K floor; $77,000-$100,000 band)`

Live source of truth: `GET /api/v1/standards` → `cover_letter.compensation` already
serves the band, floor, rule, and retired list. That endpoint beats any local file.
`profile_values.baseSalary` is now 77000.

## 2. Step 4b — Workday "use my last application" pre-fill pay audit

Insert into job-application-builder between the form-fill step and Step 6b:

```
### Step 4b - Workday "use my last application" pre-fill audit (pay figures)

Workday's "use my last application" / autofill pulls answers from PRIOR submissions,
so retired figures resurface silently. After any pre-fill, before moving on, scan every
salary/compensation field against this retired list - a hit means STOP and re-enter from
the current band ($77,000-$100,000, floor $77K):

    \$?54[,.]?000|\$?54\s?K        (old $54K hard drop)
    \$?60[,.]?000|\$?60\s?K        (old ~$60K break-even / target)
    \$?63[,.]?000|\$?63\s?K        (old $63K)
    \$?65[,.]?000|\$?65\s?K        (old $65K floor)
    70,001                            (old $70,001-$80,000 band pick)
    \$?75\s?[-]\s?\$?85\s?K|\$?75[,.]?000   (old $75-85K ask)
    \$?78[,.]?000|\$?78\s?K        (old $78K)
    \$?65\s?[-]\s?\$?70\s?K      (old $65-70K Phase 1 default)
```

## 3. verify_resume.py — retired-pay-figure guard

Append after the existing `BANNED` list, in BOTH copies
(`resume-builder/scripts/` and `job-search-sweep/scripts/` — they must stay identical):

```python
# ---- RETIRED pay figures (band updated 2026-08-22): any hit = FAIL ---------
# Current band: $77,000-$100,000, floor $77,000. If the posting's own band runs
# higher, answer inside the posting's band. These older figures must never
# appear in any resume, cover letter, or pre-filled form again.
_RP = "retired pay figure - band is $77,000-$100,000 (floor $77K), updated 2026-08-22"
BANNED += [
 (r"\$\s?54[,.]?000|\$\s?54\s?[Kk]\b", _RP + " (was: $54K hard drop)"),
 (r"\$\s?60[,.]?000|\$\s?60\s?[Kk]\b", _RP + " (was: ~$60K break-even / $60K target)"),
 (r"\$\s?63[,.]?000|\$\s?63\s?[Kk]\b", _RP + " (was: $63K)"),
 (r"\$\s?65[,.]?000|\$\s?65\s?[Kk]\b", _RP + " (was: $65K floor)"),
 (r"70,001",                                _RP + " (was: $70,001-$80,000 band pick)"),
 (r"\$\s?75\s?[-–]\s?\$?\s?85\s?[Kk]|\$\s?75[,.]?000", _RP + " (was: $75-85K ask)"),
 (r"\$\s?78[,.]?000|\$\s?78\s?[Kk]\b", _RP + " (was: $78K)"),
 (r"\$\s?65\s?[-–]\s?\$?\s?70\s?[Kk]",                   _RP + " (was: $65-70K Phase 1 default)"),
]
```

This guard is regex-only and additive — it does not touch résumé facts. The $36M and
$32-40/hr figures are résumé content, not comp targets, and are correctly untouched.

## 4. jobs-api behavior updates (server-side, already live)

Port these as bullet edits into the ACCOUNT `jobs-api` contract:

- `POST /api/v1/suggestions` accepts `snippet` as an alias for `description`.
- Suppressed suggestions return visible, dated reasons — e.g.
  `already skipped on 2026-08-22 (company+title match)`,
  `already in the tracker as interview (company+title match, application 91c5bb42)`.
  Report the reason; do not retry.
- Re-POSTing a corrected suggestion with the same normalized company+title UPDATES the
  existing card ("updated existing card") instead of creating a duplicate. Use it to fix
  pay / link / note / job_id.
- Suggestions are deduped against the applications tracker on company+title, not just
  job_id.
- `GET /api/v1/matches` returns `meta.totalRecords / returned / limit` (limit cap 1000,
  default 50) and `statusChangedAt` per match. Use `totalRecords` for read-back checks.
- Server default event stamps (`submitted_at` / `applied_at`) are Eastern-offset ISO,
  e.g. `2026-08-22T10:53:27-04:00`. Timestamps may be omitted; the server default is right.
- `job_id` parsing now covers ADP, Oracle Cloud, iCIMS, SuccessFactors, Paylocity, Taleo,
  and SmartRecruiters in addition to the prior ATSes. Always pass the employer/ATS posting
  URL so the id anchors to the ATS req id.
- Unchanged: agents never POST to `/apply-queue` (409 by design — use `/suggestions`);
  never curate the feed unprompted; Cowork uses cookie auth, no token.
